from sprites.interfaces.basic import Group


class Groups:  # TODO: Transfer to theirs own classes
    ALL = Group()
    METEORS = Group()
    ROCKETS = Group()
    PIECES = Group()
    LIFES = Group()
    OVERLAY = Group()
    ANIMATION = Group()
